# react-image-zooom@1.5.4 declares an overly-broad styled-components peer dep
# that drags react-native into the resolver. npm 7+ refuses to install with
# ERESOLVE; the legacy resolver (npm 6 behavior) handles it fine and is what
# Vercel needs in order to build this project.
legacy-peer-deps=true
